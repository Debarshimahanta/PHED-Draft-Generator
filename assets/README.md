# Assets

Optional image assets used by the application:

- `assam_emblem.jpeg`
- `chief_engineer_stamp.png`

The current generator gracefully falls back to text-only signatory output when the stamp image is absent.
