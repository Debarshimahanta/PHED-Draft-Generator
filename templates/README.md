# Templates

`CE_1` is currently generated programmatically from the approved office-letter layout. The original DOCX model can be retained here later as `CE_1.docx` if needed for archival/reference purposes.
